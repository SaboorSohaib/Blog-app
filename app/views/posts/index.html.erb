<div class="user-specific-post">
  <div class="post-info">
    <% @user.posts.each do |post| %>
      <%= link_to user_post_path(@user.id, post.id) do %>
      <% end %>
    <% end %>
  </div>
</div>
  <%= link_to  new_user_post_path(@user)  do %>
    <button type="button" class="new-post-btn">Create a New Post</button>
  <% end %>